下面我给你一个 完整、专业、可直接开发的架构方案，完全围绕你的 5 个要求，并补充我认为必须具备的功能点。
这是一个 前端优先、后端兜底、可关闭后端、可扩展、资源占用极低 的磁力解析系统设计。

我会按 架构 → 流程 → 模块 → API → 数据库 → 可选增强 的顺序写清楚。

🧩 总体架构：前端优先 + 后端兜底（可关闭）
这是你要求的核心：

✔ 前端能解析 → 绝不调用后端
✔ 前端解析失败 → 才调用后端
✔ 后端可完全关闭（前端仍可运行）
✔ 后端只做元数据解析，不参与下载（安全 + 低资源）
🏗 系统架构图（逻辑）
代码
用户浏览器
   │
   ├── ① 前端解析器（WebAssembly + WebRTC）
   │        ├─ DHT 查询（浏览器节点）
   │        ├─ Tracker 查询（HTTP/HTTPS）
   │        └─ 文件结构解析
   │
   ├── 若成功 → 返回结果（不调用后端）
   │
   └── 若失败 → 调用后端 API
                │
                ├── ② 后端解析器（Go）
                │        ├─ DHT 节点（UDP）
                │        ├─ Tracker 查询（UDP + HTTP）
                │        ├─ BT 元数据解析
                │        └─ 记录查询次数（SQLite）
                │
                └── 返回解析结果
🟩 ① 前端解析器（纯前端）
技术栈：

WebAssembly（libtorrent-wasm 或 WebTorrent Core）

WebRTC DataChannel（用于 peer 连接）

JS + WASM 解析 BT metadata

前端解析能力：

支持 magnet:?xt=

支持 HTTP/HTTPS Tracker

支持部分 DHT

支持文件列表解析

支持 infoHash 解析

前端解析失败的典型情况：

只有 UDP Tracker

冷门资源

无做种

浏览器限制连接数

前端解析失败后：
→ 自动 fallback 到后端 API（可关闭）

🟨 ② 后端解析器（Go）
特点：

只解析 metadata，不下载文件

资源占用极低（1 核 512MB 足够）

可关闭（前端仍可运行）

支持 UDP Tracker（浏览器做不到）

支持完整 DHT 节点

后端解析流程：

解析 infoHash

访问 Tracker（UDP + HTTP）

连接 peers

获取 metadata（不下载内容）

解析文件结构

返回 JSON

后端可选：

限制每分钟调用次数

限制最大并发

限制最大 metadata 大小

🟦 ③ Tracker 管理（自动 + 手动）
你的要求：

tracker 服务器可以从开源的列表自动获取，也可以手动在后台增加。

自动获取：

定时从 多个GitHub处 拉取 tracker 列表，并进行优化，只保留最少最有用的。



手动管理：

后台管理界面（简单表单）

支持：

添加 Tracker

删除 Tracker

禁用 Tracker

排序（优先级）

Tracker 存储：

SQLite 表：trackers

字段：id, url, enabled, last_success, last_fail, priority

🟫 ④ 查询统计（SQLite）
你的要求：

后台会记录每个哈希值的被查询次数，暂时用sqlite记录。

数据库表：hash_stats

字段：

id

info_hash

query_count

last_query_time

last_success

last_fail

用途：

统计热门磁力

判断是否需要缓存 metadata

限制滥用

🟧 ⑤ API 设计（支持外部调用）
你要求：

支持API进行调用查询。

我给你一个完整 API 设计：

GET /api/parse?magnet=xxx
功能：解析磁力（前端失败后调用）
返回：

json
{
  "success": true,
  "infoHash": "e04e4215...",
  "name": "Example File",
  "files": [
    {"path": "video.mp4", "size": 123456789},
    {"path": "readme.txt", "size": 1234}
  ],
  "trackers": ["udp://...", "https://..."],
  "peers": 23,
  "seeders": 12
}
GET /api/stats/:infoHash
返回某个 hash 的统计信息。

POST /api/trackers/add
添加 Tracker。

POST /api/trackers/remove
删除 Tracker。

GET /api/trackers/list
列出所有 Tracker。

🟪 ⑥ 你没提但必须有的功能（我补充）
1. 后端调用开关（你要求的）
后台配置：

代码
ENABLE_BACKEND=true/false
前端根据这个开关决定是否 fallback。

2. CORS 限制（防止别人滥用你的 API）
只允许你的前端域名访问：

代码
Access-Control-Allow-Origin: https://yourdomain.com
3. API 限流（防止被刷爆）
例如：

每 IP 每分钟 10 次

每 infoHash 每分钟 5 次

4. Metadata 缓存（可选）
缓存解析成功的 metadata：

减少重复解析

提高速度

降低资源占用

SQLite 表：metadata_cache

5. 前端优雅降级
前端逻辑：

代码
try {
   result = frontEndParse(magnet)
   if (result.success) return result
} catch {}

if (backendEnabled) {
   result = fetchBackend(magnet)
   return result
}

return {error: "无法解析"}
🧠 最终完整方案（总结）
✔ 前端解析（WASM）
解析成功 → 不调用后端

解析失败 → fallback

✔ 后端解析（Go）
可关闭

只解析 metadata

支持 UDP Tracker

支持 DHT

✔ Tracker 管理
自动从 GitHub 拉取

手动添加/删除

SQLite 存储

✔ 查询统计
每个 infoHash 记录查询次数

SQLite 存储

✔ API
/api/parse

/api/stats

/api/trackers

✔ 安全与性能
CORS 限制

限流

缓存

后端可关闭